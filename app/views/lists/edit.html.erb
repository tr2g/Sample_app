<!--編集したい投稿データをフォームに代入する場合、通常その都度タイトルや内容を指定する必要がある-->
<!--だがform_withを利用すると、インスタンス変数を渡すだけでその変数内のモデルを解読し、投稿したタイトルと内容を代入してくれる-->
<h1>編集画面</h1>

<%= form_with model: @list do |f| %>
<!--@list（インスタンス変数）をfに渡してる-->

<h4>タイトル</h4>
<%= f.text_field :title %>

<h4>本文</h4>
<%= f.text_area :body %>

<%= f.submit '保存' %>
<!--↑3つ：fにはインスタンス変数が入ってるので、その中のモデルを解読して勝手に投稿したタイトルと内容を代入してくれる-->

<% end %>